import 'package:flutter/material.dart';

class PopularEventDetail extends StatelessWidget {
  const PopularEventDetail({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
