enum MenuState {
  home,
  search,
  favorite,
  setting,
  error,
  login,
  mytickets,
  eventdetail,
  booking_success,
}

enum ViewStatus { Empty, Loading, Completed, Error, LoadMore, Refreshing }
